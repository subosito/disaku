= simple_form_for account, html: { class: 'form-horizontal' } do |f|
  = f.input :name
  = f.input :account_type, include_blank: false
  = f.input :initial_amount
  .form-actions
    = f.button :submit, class: 'btn-primary'
