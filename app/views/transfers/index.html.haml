= link_to 'New Transfer', new_transfer_path, class: 'btn btn-default pull-right'

%table.table.table-striped.table-bordered.table-responsive.m-t-large
  %thead
    %th Date
    %th.hidden-xs Title
    %th From
    %th To
    %th Amount
    %th -
  - transfers.each do |transfer|
    %tr
      %td= transfer.transfer_date
      %td.hidden-xs= transfer.title
      %td= transfer.from_account.name
      %td= transfer.to_account.name
      %td.text-right= transfer.amount
      %td.text-center= transfer.link_to_actions
